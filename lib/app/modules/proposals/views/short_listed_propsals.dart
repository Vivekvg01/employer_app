import 'package:flutter/material.dart';

class ShortListedProposal extends StatelessWidget {
  const ShortListedProposal({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('No shotlisted proposals'),
    );
  }
}
