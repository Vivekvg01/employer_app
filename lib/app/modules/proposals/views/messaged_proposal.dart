import 'package:flutter/material.dart';

class MessagedProposal extends StatelessWidget {
  const MessagedProposal({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
